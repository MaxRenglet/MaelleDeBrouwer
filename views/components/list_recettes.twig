{% set index = fn.get_permalink(fn.get_option( 'page_for_recettes' )) %}


{% include '/components/layout/title.twig' with {link:index, label:"Recettes Récentes", icon_before:"fas fa-carrot" } %}
<div class="columns is-multiline">
	{% for post in posts %}

		{% set img = fn.get_the_post_thumbnail_url(post.ID, 'post-thumbnail') %}
		{% set tags = fn.get_the_tags(post.ID) %}
		{% set cats = fn.get_the_category(post.ID) %}
		<div class="column is-4-desktop is-6-tablet is-12-mobile">
				{% include '/components/layout/card.twig' with {image: img, content:post.post_content, title:post.post_title} %}
		</div>
	{% endfor %}

</div>

{% include '/components/layout/btn.twig' with {link:index, label:'Voir plus..'} %}
